<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Bitproxy" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Bitproxy" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <!-- Custom Fonts -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
    <link href="http://fonts.googleapis.com/css?family=Lato:300,400,700,300italic,400italic,700italic" rel="stylesheet" type="text/css">
  </head>
  <body>
    <header>
      <nav>
        <div class="nav-wrapper">
          <a href="/" class="brand-logo">BITPROXY</a>
          <ul id="nav-mobile" class="right hide-on-med-and-down">
            <li>
              <a href="/" class="white-text">Home</a>
            </li>
            <li>
              <a href="/about" class="white-text">About</a>
            </li>
            <li>
              <a href="/dashboard" class="white-text">Dashboard</a>
            </li>
            <li>
              <% if user_signed_in? %>
                <a href="/users/sign_out" class="white-text">Log out</a>
              <% else %>
                <a href="/login" class="white-text">Sign up/Sign In</a>
              <% end %>
            </li>
          </ul>
        </div>
      </nav>
    </header>
      <%= render 'layouts/messages' %>
      <%= yield %>
  </body>
</html>
